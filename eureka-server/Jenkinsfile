pipeline {
    agent {
        docker {
            image 'maven:3-alpine'
            args ' -v /tmp/.m2:/tmp/.m2'
        }
    }
    dir("eureka-server/") {
        stages {
            stage('Build') {
                steps {
                    sh 'ls .'
                }
            }
        }
    }
}