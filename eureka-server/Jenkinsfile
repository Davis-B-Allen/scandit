pipeline {
    agent {
        docker {
            image 'maven:3-alpine'
            args ' -v /tmp/.m2:/tmp/.m2'
        }
    }
    stages {
        dir("eureka-server/") {
            stage('Build') {
                steps {
                    sh 'ls .'
                }
            }   
        }
    }
}